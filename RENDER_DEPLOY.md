# Деплой на Render - Подробная инструкция

## Почему Render?

Render предоставляет бесплатный план для хостинга приложений, который идеально подходит для Telegram ботов:
- ✅ Бесплатный SSL сертификат (нужен для Telegram webhook)
- ✅ Автоматическая интеграция с GitHub
- ✅ Переменные окружения для секретных данных
- ✅ Логи и мониторинг

## Ограничения бесплатного плана

⚠️ **Важно знать:**
- Приложение засыпает после 15 минут неактивности
- При получении запроса просыпается (занимает ~30 секунд)
- 750 часов работы в месяц (достаточно для одного бота 24/7)

## Способ 1: Polling режим (проще, но менее стабильно)

Используйте обычный `main.py` - бот будет постоянно опрашивать Telegram на наличие новых сообщений.

**Плюсы:** Простая настройка
**Минусы:** Бот может засыпать и пропускать сообщения

## Способ 2: Webhook режим (рекомендуется)

Используйте `main_webhook.py` - Telegram сам будет отправлять обновления на ваш сервер.

**Плюсы:** Стабильная работа, меньше нагрузка
**Минусы:** Чуть сложнее настройка

---

## Пошаговая инструкция деплоя

### Шаг 1: Подготовка GitHub репозитория

1. **Создайте новый репозиторий на GitHub:**
   - Зайдите на [github.com](https://github.com)
   - Нажмите "New repository"
   - Назовите, например: `telegram-cooldown-bot`
   - Выберите "Public" или "Private"
   - НЕ добавляйте README (он уже есть)

2. **Загрузите код в репозиторий:**

   ```bash
   # Инициализируйте git (если еще не сделали)
   git init
   
   # Добавьте все файлы
   git add .
   
   # Создайте коммит
   git commit -m "Initial commit: Telegram bot with cooldown"
   
   # Добавьте удаленный репозиторий (замените URL на ваш)
   git remote add origin https://github.com/ваш-username/telegram-cooldown-bot.git
   
   # Загрузите код
   git branch -M main
   git push -u origin main
   ```

3. **Убедитесь что .env НЕ попал в репозиторий:**
   - Проверьте что `.gitignore` содержит `.env`
   - Никогда не коммитьте токены!

### Шаг 2: Регистрация на Render

1. Откройте [render.com](https://render.com)
2. Нажмите "Get Started for Free"
3. Зарегистрируйтесь через GitHub (рекомендуется)
4. Подтвердите email

### Шаг 3: Создание Web Service

1. **На панели Render нажмите "New +"**

2. **Выберите "Web Service"**

3. **Подключите репозиторий:**
   - Нажмите "Connect account" для GitHub
   - Выберите ваш репозиторий с ботом
   - Нажмите "Connect"

4. **Настройте сервис:**

   **Basic Settings:**
   - **Name:** `telegram-bot` (или любое имя)
   - **Region:** Выберите ближайший к вам регион
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   
   **Для polling режима (обычный main.py):**
   - **Start Command:** `python main.py`
   
   **Для webhook режима (main_webhook.py):**
   - **Start Command:** `python main_webhook.py`

5. **Выберите план:**
   - Instance Type: **Free**
   - ✅ Free tier (0$/месяц)

### Шаг 4: Настройка переменных окружения

**⚠️ Самый важный шаг!**

1. **Прокрутите вниз до раздела "Environment Variables"**

2. **Добавьте переменные:**

   Нажмите "Add Environment Variable" для каждой:

   | Key | Value | Описание |
   |-----|-------|----------|
   | `BOT_TOKEN` | `ваш_токен_от_BotFather` | Токен бота (обязательно!) |
   | `MESSAGE_COOLDOWN` | `10` | Таймаут в секундах |
   | `DEBUG` | `False` | Режим отладки |

   **Только для webhook режима:**
   | Key | Value | Описание |
   |-----|-------|----------|
   | `RENDER_EXTERNAL_URL` | `https://ваш-сервис.onrender.com` | URL вашего сервиса (появится после создания) |

3. **Скопируйте токен бота:**
   - Откройте [@BotFather](https://t.me/botfather) в Telegram
   - Отправьте `/token`
   - Выберите вашего бота
   - Скопируйте токен полностью

### Шаг 5: Запуск

1. **Нажмите "Create Web Service"**

2. **Дождитесь деплоя:**
   - Render автоматически установит зависимости
   - Запустит бота
   - Процесс займет 2-5 минут
   - Следите за логами в реальном времени

3. **Проверьте логи:**
   ```
   INFO - Настройки успешно загружены
   INFO - Cooldown middleware подключен (10s)
   INFO - Роутеры зарегистрированы
   INFO - Бот запущен: @your_bot_username
   ```

### Шаг 6 (только для webhook): Установка URL

Если вы используете `main_webhook.py`:

1. **Скопируйте URL вашего сервиса:**
   - Вверху страницы будет URL типа: `https://telegram-bot-abcd.onrender.com`

2. **Добавьте переменную окружения:**
   - Environment → Add Environment Variable
   - Key: `RENDER_EXTERNAL_URL`
   - Value: `https://telegram-bot-abcd.onrender.com` (ваш URL)

3. **Сохраните - сервис перезапустится автоматически**

## Проверка работы

1. **Откройте Telegram**
2. **Найдите вашего бота** (по username)
3. **Отправьте** `/start`
4. **Должно прийти приветствие**

5. **Проверьте cooldown в группе:**
   - Создайте тестовую группу
   - Добавьте бота
   - Сделайте бота администратором
   - Попробуйте отправить несколько сообщений подряд

## Обновление бота

Когда вы обновите код:

1. **Загрузите изменения на GitHub:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```

2. **Render автоматически:**
   - Обнаружит изменения
   - Пересоберет приложение
   - Перезапустит бота

## Мониторинг и логи

1. **Просмотр логов:**
   - Откройте ваш сервис на Render
   - Перейдите в раздел "Logs"
   - Видны все сообщения в реальном времени

2. **Проверка статуса:**
   - Dashboard → Ваш сервис
   - Статус: должен быть зеленым "Live"

3. **События:**
   - Вкладка "Events" показывает историю деплоев

## Troubleshooting

### Бот не отвечает

1. **Проверьте логи:**
   - Есть ли ошибки?
   - Запущен ли бот? (должно быть "Бот запущен: @...")

2. **Проверьте переменные окружения:**
   - `BOT_TOKEN` установлен?
   - Нет лишних пробелов?

3. **Проверьте статус сервиса:**
   - Зеленый ли статус?
   - Не в режиме сна?

### Ошибка "BOT_TOKEN не установлен"

1. Перейдите в Environment Variables
2. Убедитесь что `BOT_TOKEN` добавлен
3. Нажмите "Save Changes"
4. Подождите перезапуска

### Бот засыпает (бесплатный план)

**Polling режим:**
- Это нормально на бесплатном плане
- Бот проснется при получении сообщения
- Может пропустить сообщения во время сна

**Webhook режим (решение):**
- Используйте `main_webhook.py`
- Webhook разбудит бота при получении сообщения
- Более стабильная работа

### Ошибка webhook (404, 403)

1. Проверьте `RENDER_EXTERNAL_URL`:
   - Должен совпадать с URL сервиса
   - Должен начинаться с `https://`
   - Не должен заканчиваться на `/`

2. Проверьте логи webhook:
   ```
   INFO - Webhook установлен: https://...
   ```

## Альтернативы Render

Если Render не подходит, можно использовать:

- **Railway** - похож на Render, бесплатный план
- **Heroku** - платный, но стабильный
- **PythonAnywhere** - бесплатный, для простых ботов
- **VPS** (DigitalOcean, Vultr) - от $5/месяц, полный контроль

## Дополнительные возможности Render

1. **Автоматические бекапы**
2. **Custom domains** (свой домен)
3. **Scheduled jobs** (крон задачи)
4. **Databases** (PostgreSQL, Redis)

---

**Готово! Ваш бот работает в облаке 24/7! 🎉**

Нужна помощь? Проверьте логи или создайте issue в репозитории.
