# Telegram Bot с Cooldown для групповых чатов

Telegram бот с функцией ограничения частоты сообщений (cooldown) для групповых чатов. Построен на библиотеке aiogram 3.x с модульной архитектурой для легкого расширения функционала.

> 📖 **Новичок?** Начните с [QUICKSTART.md](QUICKSTART.md) для быстрого старта!  
> 📚 **Вся документация:** См. [NAVIGATION.md](NAVIGATION.md) для навигации по всем файлам.

## 🚀 Возможности

- ✅ Работа в групповых чатах
- ⏱ Настраиваемый таймаут между сообщениями (по умолчанию 10 секунд)
- 🗑 Автоматическое удаление сообщений при нарушении cooldown
- 💬 Уведомления пользователей о необходимости подождать
- 🔧 Модульная архитектура для легкого добавления новых функций
- 🌐 Поддержка деплоя на Render (бесплатный план)

## 📁 Структура проекта

```
chatbot/
├── config/
│   └── settings.py          # Конфигурация и настройки
├── handlers/
│   ├── __init__.py
│   ├── commands.py          # Обработчики команд
│   └── group.py             # Обработчики групповых сообщений
├── middlewares/
│   ├── __init__.py
│   └── cooldown.py          # Middleware для cooldown
├── main.py                  # Точка входа приложения
├── requirements.txt         # Зависимости Python
├── runtime.txt              # Версия Python для Render
├── render.yaml              # Конфигурация для Render
├── .env.example             # Пример переменных окружения
└── .gitignore
```

## 🛠 Установка и запуск локально

### Требования

- Python 3.11+
- pip

### Шаги установки

1. **Клонируйте репозиторий или скачайте файлы**

2. **Создайте виртуальное окружение**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение**
   
   Windows (PowerShell):
   ```powershell
   .\venv\Scripts\Activate.ps1
   ```
   
   Windows (CMD):
   ```cmd
   venv\Scripts\activate.bat
   ```
   
   Linux/Mac:
   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

5. **Настройте переменные окружения**
   
   Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```
   
   Отредактируйте `.env` и укажите ваш токен бота:
   ```env
   BOT_TOKEN=your_bot_token_here
   MESSAGE_COOLDOWN=10
   DEBUG=False
   ```

6. **Получите токен бота**
   
   - Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен в файл `.env`

7. **Запустите бота**
   ```bash
   python main.py
   ```

## 🌐 Деплой на Render (бесплатно)

Render предоставляет бесплатный план для хостинга ботов.

### Шаг 1: Подготовка репозитория

1. Создайте репозиторий на GitHub
2. Загрузите все файлы проекта (убедитесь что `.env` НЕ попал в репозиторий!)
3. Убедитесь что файлы `render.yaml` и `runtime.txt` присутствуют

### Шаг 2: Создание сервиса на Render

1. **Зарегистрируйтесь на [Render](https://render.com/)**

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Render автоматически обнаружит `render.yaml`

3. **Настройте переменные окружения:**
   
   В разделе "Environment" добавьте:
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   MESSAGE_COOLDOWN=10
   DEBUG=False
   ```

4. **Настройте сервис:**
   - **Name:** telegram-bot (или любое имя)
   - **Environment:** Python
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`
   - **Plan:** Free

5. **Нажмите "Create Web Service"**

Render автоматически соберет и запустит ваш бот. Процесс займет 2-5 минут.

### Важные заметки по Render

⚠️ **Бесплатный план Render:**
- Автоматически засыпает после 15 минут неактивности
- Пробуждается при получении HTTP запроса (может занять 30-60 секунд)
- Для Telegram ботов рекомендуется использовать webhook вместо polling

### Альтернатива: Webhook для Render

Для стабильной работы на бесплатном плане Render, можно переключиться на webhook режим. Создайте файл `main_webhook.py`:

```python
from aiohttp import web
from aiogram.webhook.aiohttp_server import SimpleRequestHandler, setup_application

async def on_startup(app):
    await bot.set_webhook(f"https://ваш-домен.onrender.com/webhook")

async def on_shutdown(app):
    await bot.delete_webhook()

# Добавьте в main()
app = web.Application()
app.on_startup.append(on_startup)
app.on_shutdown.append(on_shutdown)

SimpleRequestHandler(dispatcher=dp, bot=bot).register(app, path="/webhook")
setup_application(app, dp, bot=bot)
web.run_app(app, host="0.0.0.0", port=10000)
```

## 📋 Команды бота

- `/start` - Приветственное сообщение
- `/help` - Справка по использованию
- `/status` - Статус бота (только для админов групп)

## ⚙️ Настройка

Все настройки находятся в переменных окружения (файл `.env`):

- `BOT_TOKEN` - Токен Telegram бота (обязательно)
- `MESSAGE_COOLDOWN` - Время cooldown в секундах (по умолчанию: 10)
- `DEBUG` - Режим отладки (True/False)

## 🔧 Добавление новых функций

Благодаря модульной архитектуре, легко добавлять новые функции:

### Добавление новой команды

1. Откройте `handlers/commands.py`
2. Добавьте новый обработчик:

```python
@command_router.message(Command("mycommand"))
async def cmd_mycommand(message: Message):
    await message.answer("Мой новый функционал!")
```

### Добавление нового роутера

1. Создайте новый файл в `handlers/`, например `handlers/moderation.py`
2. Создайте роутер и обработчики
3. Добавьте роутер в `handlers/__init__.py`
4. Подключите в `main.py`: `dp.include_router(moderation_router)`

### Добавление нового middleware

1. Создайте файл в `middlewares/`
2. Реализуйте класс наследуясь от `BaseMiddleware`
3. Подключите в `main.py`: `dp.message.middleware(YourMiddleware())`

## 🐛 Отладка

Включите режим отладки в `.env`:
```env
DEBUG=True
```

Это активирует подробное логирование всех операций бота.

## 📝 Логи

Бот логирует важные события:
- Запуск и остановка
- Обработка сообщений
- Срабатывание cooldown
- Ошибки

На Render логи доступны в разделе "Logs" вашего сервиса.

## 🔒 Безопасность

- ❌ Никогда не коммитьте `.env` файл с токенами
- ✅ Используйте переменные окружения на Render
- ✅ Ограничьте права бота в группах (только необходимые)
- ✅ Регулярно обновляйте зависимости

## 📦 Зависимости

- `aiogram>=3.4.0` - Асинхронная библиотека для Telegram Bot API
- `python-dotenv>=1.0.0` - Загрузка переменных окружения из .env

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь что токен правильный
3. Проверьте права бота в группе
4. Убедитесь что бот добавлен как администратор (для удаления сообщений)

## 📄 Лицензия

MIT License - используйте свободно для любых целей.

## 🎯 Roadmap для расширения

Идеи для дальнейшего развития:
- [ ] Антиспам фильтры
- [ ] Модерация контента
- [ ] Статистика сообщений
- [ ] Персональные настройки cooldown для ролей
- [ ] База данных для хранения настроек
- [ ] Веб-панель управления
- [ ] Поддержка нескольких языков

---

**Удачи с вашим ботом! 🤖**
