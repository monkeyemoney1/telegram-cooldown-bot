# Быстрый старт 🚀

## 1. Создайте бота в Telegram

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Введите имя бота (например: "My Cool Bot")
4. Введите username бота (должен заканчиваться на "bot", например: "my_cool_bot")
5. Скопируйте полученный токен (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## 2. Настройте проект

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` и вставьте ваш токен:
   ```env
   BOT_TOKEN=ваш_токен_от_botfather
   MESSAGE_COOLDOWN=10
   DEBUG=False
   ```

## 3. Установите зависимости

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows PowerShell:
.\venv\Scripts\Activate.ps1

# Windows CMD:
venv\Scripts\activate.bat

# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## 4. Запустите бота

```bash
python main.py
```

Вы должны увидеть:
```
INFO - Настройки успешно загружены
INFO - Cooldown middleware подключен (10s)
INFO - Роутеры зарегистрированы
INFO - Webhook удален
INFO - Бот запущен: @your_bot_username
```

## 5. Проверьте работу

1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start` - должно прийти приветствие
3. Создайте тестовую группу и добавьте бота
4. **Важно:** Сделайте бота администратором группы (нужно для удаления сообщений)
5. Попробуйте отправить несколько сообщений подряд - увидите работу cooldown

## 6. Деплой на Render

### Подготовка

1. Создайте аккаунт на [render.com](https://render.com)
2. Загрузите проект на GitHub (НЕ ВКЛЮЧАЙТЕ `.env` файл!)

### Деплой

1. На Render: New → Web Service
2. Подключите GitHub репозиторий
3. Настройки будут взяты из `render.yaml`
4. **Обязательно** добавьте переменную окружения `BOT_TOKEN` в разделе Environment
5. Нажмите "Create Web Service"

Готово! Ваш бот работает в облаке 24/7 (на бесплатном плане с ограничениями).

## Настройка прав бота в группе

Для корректной работы бот должен иметь права:
- ✅ Delete messages (удаление сообщений)
- ✅ Send messages (отправка сообщений)

Как дать права:
1. Добавьте бота в группу
2. Группа → Настройки → Администраторы → Добавить администратора
3. Выберите бота
4. Установите права "Delete messages" и "Send messages"

## Проблемы?

### Бот не отвечает
- Проверьте что токен правильный
- Проверьте логи: должно быть "Бот запущен: @..."
- Убедитесь что бот не заблокирован в Telegram

### Бот не удаляет сообщения в группе
- Проверьте что бот - администратор группы
- Проверьте что у бота есть право "Delete messages"

### Ошибка "BOT_TOKEN не установлен"
- Проверьте файл `.env`
- Убедитесь что виртуальное окружение активировано
- На Render: проверьте переменные окружения

## Следующие шаги

- Изучите `README.md` для подробной документации
- Добавьте новые команды в `handlers/commands.py`
- Настройте время cooldown в `.env`
- Расширьте функционал используя модульную структуру

**Удачи! 🎉**
